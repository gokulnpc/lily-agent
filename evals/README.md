# evals

Golden dataset (≥100 cases) + eval runners. CI gate: hard assertions (compatibility
matches SQL truth, zero invalid part numbers) must pass 100%; LLM-judge scores must
not regress >2% (NFR-23/24).

**Phase 2/5.** Placeholder only — `make evals` is a stub until then.
