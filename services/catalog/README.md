# catalog

Parts / models / compatibility API over Aurora PostgreSQL. Source of truth for every
part fact the agent states (prices, stock, fitment). Hosts the deterministic
part-number validator.

**Phase 1/2.** Placeholder only — no code yet.
