apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "etl-crawl.name" . }}-config
  labels:
    {{- include "etl-crawl.labels" . | nindent 4 }}
data:
  LILY_RAW_BUCKET: {{ .Values.config.rawBucket | quote }}
  AWS_REGION: {{ .Values.config.awsRegion | quote }}
