global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment once the gateway runs locally with a /metrics endpoint (Phase 2).
  # - job_name: gateway
  #   static_configs:
  #     - targets: ["host.docker.internal:8000"]
