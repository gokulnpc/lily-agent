# DECISIONS.md — Locked Architecture & Phase Plan

Status legend: **LOCKED** = do not change without owner sign-off · **OPEN** = needs owner input before implementing.

## Locked decisions

| # | Decision | Choice | Rationale (short) |
|---|---|---|---|
| D1 | LLM provider | Claude on AWS Bedrock (Converse API) | LOCKED. IAM-native auth, single AWS boundary |
| D2 | Model tiering | Haiku: guardrails/router/rewrites · Sonnet: specialist agents | LOCKED. Cost + latency |
| D3 | Embeddings | Titan Embeddings v2 via Bedrock | LOCKED |
| D4 | Orchestration | LangGraph; Redis checkpointer; tools framework-agnostic | LOCKED |
| D5 | Agent topology | input guardrail → intent router → specialists (Product/Compatibility/Repair/Order) → deterministic validator → output guardrail | LOCKED |
| D6 | Guardrails | Bedrock Guardrails (scope, PII) + custom Haiku classifier + deterministic part-number validator | LOCKED |
| D7 | Compute | EKS, real AWS. Namespaces: frontend, agent, commerce, data, observability, platform | LOCKED |
| D8 | Edge | Route53 → CloudFront → WAF → ALB ingress | LOCKED |
| D9 | Primary DB | Aurora PostgreSQL Serverless v2 (0.5 ACU floor) | LOCKED |
| D10 | Search/RAG + logs | One OpenSearch domain; separate index namespaces for retrieval vs logs; hybrid BM25+kNN | LOCKED |
| D11 | Cache/session | ElastiCache Redis: sessions, semantic cache (cosine ≥0.95 + intent match), rate limits | LOCKED |
| D12 | Ingestion | discovery → SQS → fetchers (Playwright/HTTP) → raw S3 (versioned) → parsers (read S3 only) → Aurora → embeddings → OpenSearch. Content-hash change detection, nightly incremental | LOCKED |
| D13 | Crawl scope (initial) | ~500 representative fridge+dishwasher pages; widen only in Phase 5 | LOCKED |
| D14 | Observability | OTel everywhere → Jaeger; Prometheus+Grafana; Fluent Bit → OpenSearch/Kibana; Alertmanager → notifications svc → SNS/Slack; Langfuse for LLM traces | LOCKED |
| D15 | IaC / CI | Terraform (modules + envs), GitHub Actions, ECR | LOCKED |
| D16 | Frontend | Next.js App Router, SSE streaming, PartSelect branding | LOCKED |
| D17 | Cost guards | Spot node groups (stateless), single NAT, overnight scale-down for non-prod, hourly LLM budget alert | LOCKED |
| D18 | Orders | Mock order service; no real payments; cart hands off to PartSelect checkout deep link | LOCKED |

## Open decisions (ask owner before implementing)
| # | Question |
|---|---|
| O1 | Crawl breadth at launch: all brands vs top-N by catalog size |

## Resolved decisions
| # | Decision | Date |
|---|---|---|
| O4 | **GitHub Actions deploys only** in MVP — no Argo CD. `k8s/` stays pure app charts (Argo-compatible); Argo Rollouts considered for Phase 5 canaries. | 2026-06-11 |
| O3 (resolved) — Langfuse | **SKIPPED.** No Langfuse pod. The LLM-observability story (NFR-20) is carried by **Jaeger spans** (a `bedrock.converse` child span per call with model id + `gen_ai.usage.*` token counts), **token/cost Prometheus metrics** (`lily_bedrock_tokens_total{model,direction}`, `lily_bedrock_cost_usd_total{model}` in `libs/common/metrics.py`), and the **structured logs**. Conscious tradeoff: no prompt/completion capture (size/PII) — Jaeger + metrics + logs are the LLM-obs surface. Revisit only if prompt-version eval tooling is needed. | 2026-06-12 |
| O5 (resolved) — Voice input | **CUT** from demo scope (P2 stays backlog; architecture still allows a voice channel later — D-extensibility). | 2026-06-12 |
| Phase 4 deploy home | **Observability stack = Terraform `helm_release` in `terraform/envs/dev/platform`** (control-plane tier — the prometheus-operator ships an admission webhook, same class as the ALB/cert-manager/ESO webhooks). Reuses the locked `atomic/cleanup_on_fail/wait/timeout=600 + depends_on=[alb_controller] + explicit system-pool webhook pinning` hardening. O4-compliant: it is NOT a `k8s/` app chart. Values under `k8s/values/observability/`. | 2026-06-12 |
| Phase 4 — no OTel Collector | **Gateway exports OTLP directly to Jaeger's native receiver** (`OTEL_EXPORTER_OTLP_ENDPOINT` → Jaeger OTLP HTTP), no standalone Collector pod — one fewer pod on the tight 3× t3.medium cluster; dev needs no fan-out/sampling. The exporter is **env-gated** in `setup_tracing` (offline/tests stay exporter-free). | 2026-06-12 |
| Phase 4 — Alertmanager → Slack | **Direct Slack incoming-webhook receiver** (URL in AWS Secrets Manager → ESO `ExternalSecret` → Alertmanager `slack_configs.api_url_file`), NOT the SNS/notifications-svc fan-out (NFR-21's full design stays Phase-5/later). A watchdog `PrometheusRule` proves the path. Owner provides the webhook URL before the 4c apply. | 2026-06-12 |
| Phase 4 — Fluent Bit → OpenSearch (4c findings) | Live bring-up findings: **(1) Scoped-IAM bulk writes WORK** — `es:ESHttpPost` on `domain/lily-dev/_bulk` + manage `lily-logs*` is sufficient for SigV4 bulk ingest (the gate's "bulk IAM might not work" concern was unfounded; observed HTTP 200 on `/_bulk`). **(2) Ship only the Lily APP namespaces** (`agent|frontend|data|commerce`) — NOT all of kube-system (429-throttled the tiny dev domain) and NOT `observability` itself (Fluent Bit re-ingesting its own logs = a feedback loop; ~250k self-logs before the fix). **(3) Merge app JSON to the ROOT** of the doc (dropped `Merge_Log_Key app`) so `trace_id`/`session_id`/`intent` are top-level fields (NFR-19, Kibana-queryable) — the log↔trace join filters `lily-logs-*` by `trace_id` and matches the Jaeger `lily.trace_id` tag. **(4) Read-isolation IS IAM-enforced** (orchestrator scoped to `retrieval-*`, can't read `lily-logs-*`); bulk-WRITE isolation is naming-based only (the `/_bulk` body isn't IAM-restrictable — FGAC would be needed). The lily-logs index carries one-time bring-up noise (the debug/flood period) that ages out via daily indices. | 2026-06-12 |
| Phase 4 — observability data plane on system pool | **Changed from the 4b sketch's "data plane on spot".** Prometheus/Grafana/Alertmanager/Jaeger run on the on-demand `system` pool (the operator + admission webhooks already must, per the convention). Monitoring must not share the single reclaimable `stateless` (spot) node with the very app pods it watches — a spot loss would take down both. Verified at apply: data plane landed on the 2 system nodes (~1.4Gi of 5.5Gi free); the spot node carries only the Fluent Bit + node-exporter daemonsets + the apps. | 2026-06-12 |
| Phase 4 CLOSED | **Exit met (4 live proofs).** (1) End-to-end Jaeger trace: `chat.turn` → `graph.{entry,input_guardrail,router,specialist,validator,output_guardrail,save}` → 4× `bedrock.converse` with `gen_ai.request.model` + `gen_ai.usage.{input,output}_tokens`. (2) 4 Grafana dashboards live (`grafana.dev.lily-agent.com`, login-only) — cost-per-conversation drawing from `lily_bedrock_tokens_total`/`lily_bedrock_cost_usd_total` (~$0.008/conversation). (3) Log↔trace join proven (one turn: OpenSearch `lily-logs-*` doc + Jaeger trace both carry the same `lily.trace_id`). (4) Watchdog alert delivered to Slack (`alertmanager_notifications_total{slack}=1`, `failed=0`). | 2026-06-12 |
| — | Dev region: **us-east-1** (Bedrock availability; ACM certs reusable for CloudFront). | 2026-06-11 |
| — | Phase 0 edge: ACM cert on ALB; **CloudFront + WAF deferred to Phase 3** (D8 target topology unchanged). See docs/adr/0001. | 2026-06-11 |
| D2 (amended) | Model tiering moved to **current generation: Haiku 4.5 (router/guardrails) + Sonnet 4.6 (specialists)**. The 3.5 generation is legacy-gated on Bedrock — `anthropic.claude-3-5-haiku` returns "upgrade to an active model" on invocation. Exact inference-profile IDs (from `aws bedrock list-inference-profiles`): router `global.anthropic.claude-haiku-4-5-20251001-v1:0` (the Global profile confirmed active in the account), specialists `global.anthropic.claude-sonnet-4-6`. On-demand bare model IDs aren't invocable — must use the inference profile (NFR-5 cross-region). Defaults in `services/orchestrator/.../converse.py`, env-overridable via `LILY_ROUTER_MODEL_ID` / `LILY_SPECIALIST_MODEL_ID`. | 2026-06-12 |
| D9 (amended) | Aurora Serverless v2 floor changed **0.5 ACU → 0 ACU with auto-pause** (10 min idle, ~15s cold resume). The 0-ACU feature postdates the original decision; D9's rationale was cost, and this is ~$40/mo cheaper for a dev cluster. Floor is the `aurora_min_acu` tfvar — raise it if cold starts ever matter. | 2026-06-12 |
| D12 (crawler) | **Compatibility pairs ingested model-page-only** (A9 model-canonical); part pages = attributes only. **Fetcher uses Playwright Chrome channel** (headless is Akamai-403'd) with identified UA, robots honored, rate-limited — access requirement, no proxies/spoofing. **Seed mix = complete models + the parts they reference + relevant symptom pages**, ~500 cap, sitemap-driven discovery (no link-walking). Widening = Phase 5 + owner confirm. | 2026-06-12 |
| symptom_parts source (A14 resolved) | **`symptom_parts` is sourced from part-DETAIL pages' source-attested "This part fixes the following symptoms" list** (PartSelect's own part→symptom linkage), NOT a heuristic. The **section-name heuristic is rejected.** Path: crawl the part detail pages → persist `symptoms_fixed` → map each phrase to a `catalog.symptoms.name` via a **small curated vocab table** (the one human-judgment artifact; reviewed before upsert, unmatched phrasings logged not force-matched) → upsert `symptom_parts`. This also closes the bigger gap (768/770 parts currently have no price/stock/difficulty/install). Supersedes A14's "deferred". | 2026-06-12 |
| Install routing (gap close) | **Keep the route `install → repair`** (no router/taxonomy change). Close it from the data side: once part pages are crawled, give the Repair specialist a **`get_install_info(ps_number)`** tool backed by part attributes (difficulty, time, video URL). | 2026-06-12 |
| FR-17 ranking (amended) | **Per-part fix percentages do not exist at the source** (A5 refuted; part pages list symptoms without %). `diagnose_symptom.likely_parts` therefore **ranks by an honest fallback signal (review_count / in-stock), or returns unranked with an explicit disclosure** in the output framing — never a fabricated fix %. `symptom_parts.fix_percentage` stays NULL. | 2026-06-12 |
| In-cluster retrieval proof | **Deferred to Phase 5** (the gateway step builds the orchestrator image anyway). The disclosed local OpenSearch stand-in (`specialist_demo.py`) remains acceptable for proofs until then. | 2026-06-12 |
| D9 (amended 2026-06-12) — pause vs. always-warm | **The long-lived gateway's persistent DB connection floors Aurora at 0.5 ACU while it runs — accepted deliberately.** Aurora 0-ACU auto-pause needs zero connections; the embedded orchestrator holds one (dev-scale single connection), so the cluster never pauses to 0 while the gateway is up (verified 5d: 0.5 ACU through an 11-min idle, DatabaseConnections=1, first-after-idle request 9.8s with no resume penalty). The alternative — connection-per-request so Aurora can pause — would impose a ~10s cold-start on the first request after each idle, which is the worse trade for an interactive chat. **The 0-ACU saving (D9) is realized instead through the D17 overnight scale-down windows**: nightly scale-down takes the gateway (and its connection) to zero, Aurora then pauses, and the morning's first request rides the resume via `connect_with_retry`. So scale-down — not idle auto-pause — is what carries the cost saving now. | 2026-06-12 |
| A11 / Aurora pause vs. the long-lived gateway (5d finding) | **A held DB connection prevents 0-ACU auto-pause.** The deployed gateway (embedded orchestrator, dev-scale single connection) keeps exactly 1 Aurora connection open via its cached graph. Aurora Serverless v2 auto-pause requires ZERO connections, so with the gateway up Aurora floors at **0.5 ACU**, never pausing to 0 (verified: 11-min idle held 0.5 ACU, DatabaseConnections=1; first request after idle = 9.8s, no resume penalty). Net: the running service faces **no cold start** (good for latency), but the 0-ACU cost saving (D9) doesn't apply while the gateway runs (~0.5 ACU floor ≈ +$43/mo). The cold-start path (`connect_with_retry` on graph build / the migrate init) is exercised only on a FRESH start against a paused Aurora — e.g. the morning after the D17 overnight scale-down. This deploy's migrate ran against a warm Aurora (0 init restarts), so a true paused→resume-on-connect wasn't witnessed; the retry is in place for it. | 2026-06-12 |
| Redis checkpointer needs Redis Stack (D11) | The langgraph `AsyncRedisSaver` (D11 checkpointer) uses **RediSearch** (`FT.*` commands) to index checkpoints — plain `redis` 403s with "unknown command FT._LIST". The in-cluster Redis must be `redis/redis-stack-server` (RediSearch + RedisJSON), and the saver must be the **async** one (`langgraph.checkpoint.redis.aio.AsyncRedisSaver`) because the SSE path drives the graph with `astream`; the sync `RedisSaver` raises `NotImplementedError` on `aget_tuple`. | 2026-06-12 |
| O2 (resolved) — demo order data | **Seeded fixtures, not free-form mock.** `db/migrations/0007_demo_orders.sql` seeds 4 deterministic demo orders (all email `demo@lily.test`): `LILY-1001` shipped+UPS-tracking, `LILY-1002` delivered (full timeline), `LILY-1003` processing/2-items, `LILY-1004` return_requested — varied statuses, realistic timelines, real catalog PS numbers (soft-linked). Idempotent (CTE skips items/events on order conflict). The (order#, email) pairs are documented in the migration header for the demo script. | 2026-06-12 |
| ProductCard: no fix_percentage (3a) | Dropped `fix_percentage` from the ProductCard schema — it is permanently NULL (FR-17: no per-part fix rates at the source; repair ranking uses review count). Adding a field later is cheap; a vestigial always-null one isn't. | 2026-06-12 |
| Repair likely-parts ranking (deferred → Phase 5) | A section-family-relevance re-rank (prefer parts whose schematic section matches the symptom, review-count tiebreak) would fix the visible "Crisper Drawer leads the ice-maker cards" artifact, BUT it is **not a cheap join**: section affiliation is only embedded in `part_model_compatibility.source_url` (no clean column; a part has multiple affiliations via different models), and there is **no symptom→section map** — that would be a new curated human-judgment table (like `symptom_vocab`, ~21 symptoms, gated review) plus section-slug extraction/storage + a re-backfill of `symptom_parts.display_rank`. Reported as Phase-5 work; current ranking stays review-count (FR-17). | 2026-06-12 |
| SSE: no token-level streaming (D5/FR-4) | **The chat SSE protocol has NO token-delta event, by design.** Streaming the assistant text token-by-token would put part numbers in front of the user BEFORE the deterministic validator has seen the complete response — breaking FR-4 (every PS/model number is validated against the catalog before it reaches a user). So the wire carries per-graph-node **status** events (which also cover perceived latency) and a single final **message** event after the validator + output guardrail. `citations` (FR-19) and `invalid_identifiers` (FR-4) ride on that message, pulled structurally from tool results — the frontend never parses URLs/IDs from prose. Revisit only if a validated-prefix streaming scheme is built. | 2026-06-12 |
| D4/D5 (amended) — orchestrator deployment | **Orchestrator embedded in the gateway process** for dev/demo scale (step 5): the FastAPI gateway builds the LangGraph graph in-process and serves SSE — one deployable, one IRSA role, no inter-service hop. The **`build_prod_graph()` factory preserves the seam** (Deps + checkpointer injection, framework-agnostic tools), so splitting the orchestrator into its own service later is a **deploy/packaging change, not a refactor**. D5 topology (guardrail→router→specialists→validator→guardrail) is unchanged. | 2026-06-12 |
| Migrate-on-deploy (standing convention) | **Idempotent, forward-only migrations run as the FIRST step of every deploy/Job that touches the DB** — not a one-off for the crawl. Established by the `etl-crawl` Job (`python -m lily_db.migrate` before enqueue-parts/fetch). **Future DB-touching services (gateway, orchestrator, etl) adopt the same migrate-first shape** (init step / job pre-step) so schema is always current before code that depends on it runs. `lily_db.migrate` is already idempotent (tracked in `public.schema_migrations`) and safe to run on every start. | 2026-06-12 |
| Guardrail PII handling (D6 / NFR-13) | Bedrock Guardrail PII policy is **ANONYMIZE, not BLOCK**, for EMAIL/PHONE/CREDIT_DEBIT_CARD/SSN. The order#/email are resolved in the graph **entry node, BEFORE the guardrail**, and used only by the **deterministic order tool** (parameterized SQL) — never sent to an LLM in raw form. The LLM (router/specialist) and the rendered response see only the **masked** values. So order lookups keep working while PII never reaches the model or the user-visible output. Guardrail id/version injected via `LILY_GUARDRAIL_ID`/`LILY_GUARDRAIL_VERSION`; unset ⇒ Haiku scope gate alone (offline/dev). | 2026-06-12 |

## Phase 5 / orchestrator-deploy checklist (open items)

1. **Orchestrator IRSA role** (agent namespace) — does not exist yet (the etl role only has Titan-embeddings InvokeModel). At deploy it needs: `bedrock:ApplyGuardrail` on the guardrail ARN (`arn:aws:bedrock:us-east-1:<acct>:guardrail/3j8onmgxdt8v`) **and** `bedrock:InvokeModel` on the Haiku 4.5 + Sonnet 4.6 inference profiles. Add an `irsa_orchestrator` module (SA `orchestrator` in `agent`) mirroring `irsa_etl`. Wire `LILY_GUARDRAIL_ID=3j8onmgxdt8v` / `LILY_GUARDRAIL_VERSION` into the orchestrator config.
2. **Orchestrator image + ECR repo** — `repository_names` in `infra/main.tf` is `["gateway","crawler","etl"]`; add `orchestrator`. The SSE gateway either embeds the LangGraph orchestrator or calls it as a sidecar/service — decide at plan time. Image bundles `lily_db` migrations (migrate-on-deploy).
3. **In-cluster retrieval proof (deferred from step 3)** — `diagnose_symptom` / `search_parts` use the VPC-only OpenSearch; the local `specialist_demo.py` used a disclosed stand-in. Once the orchestrator image exists, run the three brief examples in-cluster (real OpenSearch + Aurora + Bedrock + the now-populated `symptom_parts`) to retire the stand-in.
4. **Migrate-on-deploy** — the orchestrator/gateway image runs `python -m lily_db.migrate` as its first step (standing convention), so schema (incl. `symptom_parts`, vocab) is current before the graph serves traffic.
5. **Guardrail published-version re-cut** — the live guardrail (DRAFT) is current, but the pinned `aws_bedrock_guardrail_version` (`1`) still reflects the pre-topic-drop config (the `replace_triggered_by` did not re-cut on the in-place update). Publish a fresh version before prod pins one; dev uses DRAFT, which is current.

### Phase 5 backlog (quality)
- **Validator MPN false-positives (FR-4 precision).** The deployed validator flags manufacturer part numbers (`WPW10321304`, `W10854221`, filter codes like `EDR1RXD1`) as invalid because its model-shaped regex checks tokens only against `catalog.models`, while MPNs live in `parts.mfr_part_number` / part names. These are REAL catalog identifiers the grounded LLM echoed from tool results — not hallucinations — so no bad id reached a user, but `invalid_identifiers` is noisy and a strict regenerate-on-invalid policy would misfire. **Fix direction:** validate model-shaped tokens against the identifiers the TOOLS returned this turn (grounding guarantees the LLM only echoes tool-sourced ids) and/or `parts.mfr_part_number` — flag only ids absent from both the turn's tool data and the catalog. Surfaced in the 5d proof; owner to decide fix-now vs Phase 5.

### Phase 5 backlog (data)
- **Ice-dispensing canonical symptom (vocab gap).** The phase-2 harvest found a 33-occurrence ice-**dispensing** cluster in part `symptoms_fixed` — `ice maker won't dispense ice` (26), `dispenses too little ice` (6), `dispenses too much ice` (1) — with **no canonical symptom** to map to (the repair index only has "Ice maker not making ice", i.e. ice *making*). Left UNMATCHED through two review rounds (not force-matched). **Phase-5 option:** add a curated parts-only canonical symptom for ice dispensing — defensible because the part→symptom linkage is source-attested on the part pages — and map the cluster to it. Not a force-match; a Phase-5 decision.

### Resolved during step 4 (2026-06-12)
- **Bedrock topic policy DROPPED (D6).** The live two-layer run found the `topicPolicy` false-positived on legitimate brand+model part questions ("which door bin fits a Frigidaire FFHS2611LWE?" → wrongly blocked). The Haiku scope gate is the accurate scope arbiter (6/6, incl. "best fridge brand"→block and the door-bin question→pass). Decision: **keep Bedrock for what it does irreplaceably — PROMPT_ATTACK (caught both injections at `contentPolicy`) + PII anonymize — and leave scope-gating to the Haiku gate.** Re-applied; demo confirms zero false positives.

## Phase 0 hardening notes (2026-06-11)

Three failures hit during first platform bring-up; each fix is now in
`terraform/envs/dev/platform/main.tf` and verified by a clean destroy → single-pass
re-apply (112s):

| Fix | Failure it addresses |
|---|---|
| `atomic + cleanup_on_fail + wait` on every `helm_release` | A failed install stranded the release in `failed` state, blocking every retry until manual `helm uninstall`. Atomic installs roll back to nothing. |
| `depends_on = [helm_release.alb_controller]` on cert-manager and external-secrets | The ALB controller's **mutating webhook intercepts pod creation cluster-wide**. Charts installed concurrently with it raced its unready endpoints ("no endpoints available for service aws-load-balancer-webhook-service") and failed. Install the controller alone, then everything else. |
| ESO cert-controller readiness window widened (period 10s × threshold 12 ≈ 2 min) | The cert-controller reports ready only after provisioning the webhook TLS secret. Chart 2.6.0's default window (20s delay + 3×5s) is too tight on first install; the probe flapped and stalled `helm --wait` until timeout. |

Related convention (also in CLAUDE.md): admission webhooks and control-plane-critical
pods are pinned to the on-demand `system` node pool — a spot reclaim of a webhook pod
turns into cluster-wide admission failures. The chart-level `nodeSelector` does NOT
cover webhook/cainjector/cert-controller subcomponents; each needs its own pin.

## Phase 1 schema assumptions — VERIFY DURING CRAWLER STEP (2026-06-12)

The 0001 schema was designed without real PartSelect HTML in hand (the site 403s
plain fetchers). Each assumption below is hedged in the DDL (`db/migrations/0001_init.sql`)
but gets tested for real by the crawler/parser. **Update the Status column as real
pages confirm or refute each one; a refuted assumption gets an additive migration,
recorded here.**

| # | Assumption | Hedge in schema | Status |
|---|---|---|---|
| A14 | **symptom_parts can't be backfilled from part-page `symptoms_fixed`.** Discovered Phase 2 (2026-06-12): the part parser extracts `symptoms_fixed` but it is **never persisted** (no `catalog.parts` column; `upsert_part` drops it), the part-page vocabulary differs from the repair-index symptom names ("Ice maker won't dispense ice" vs canonical "Ice maker not making ice"), and only 2 part pages were crawled — so a `symptoms_fixed → symptoms.name` join links almost nothing. **The authoritative FR-17 source is the symptom DETAIL pages** (`/Repair/{appliance}/{symptom}/`, which list the parts that fix each symptom + fix %); no parser exists for them yet and they weren't crawled. **Deferred → still deferred after verification (2026-06-12):** the symptom-detail-page approach was capture-tested and **refuted** (A5) — those pages list part *categories*, not `PS#` parts or fix %. So `symptom_parts` can't be populated from them either. Remaining options for a future step: (1) **section-name heuristic** — map a symptom to a schematic section name (e.g. "Not making ice" → the model's `Ice-Maker-Parts` section) and use the already-crawled section parts (model-specific, real `PS#`, no fix %, no new crawl); (2) symptom→category→category-page crawl (not model-specific). Until then `symptom_parts` stays empty and `diagnose_symptom` returns matched symptoms + its note. **RESOLVED 2026-06-12 (owner): source = part-DETAIL `symptoms_fixed` (source-attested) via a curated vocab map; section-name heuristic rejected — see Resolved decisions "symptom_parts source". Enabled by the Step 3b part-detail enrichment crawl.** | resolved |
| A1 | MPN uniqueness scope (global? per-brand?) | non-unique index on `mfr_part_number_norm`; MPN lookups may return multiple candidates, agent disambiguates | untested |
| A2 | `model_number` globally unique | `UNIQUE(model_number_norm)`; ETL logs conflicting-brand upserts as schema drift; fallback = unique(brand, model_number_norm) + disambiguation turn | untested |
| A3 | difficulty/time vocab ("Really Easy", "15-30 mins") | verbatim text, no CHECK, never branch on these | untested |
| A4 | one install video per part | single `install_video_url`; multiples ⇒ additive `part_videos` table | untested |
| A5 | fix % present per symptom-part pair | nullable + `display_rank` fallback ordering | **REFUTED 2026-06-12.** Symptom detail pages (`/Repair/{appliance}/{symptom}/`, e.g. Not-Making-Ice) carry **no per-part fix percentages and no `PS#` part links at all** — they list part **categories** (e.g. "Water Inlet Valve") linking to category pages (`/Refrigerator-Hoses-and-Tubes.htm`). So `symptom_parts.fix_percentage` will always be NULL, and symptom→specific-part can't be sourced from detail pages. See A14. |
| A6 | stock label vocabulary | raw `stock_status` text + parser-derived `in_stock` boolean; unknown ⇒ NULL ⇒ "check product page" | untested |
| A7 | qna/review shapes (no stable IDs, unanswered Qs, anon reviewers) | nearly all nullable; dedup via `UNIQUE(part_id, content_hash)` | untested |
| A8 | USD only; sale + list price | `price_usd` + nullable `list_price_usd`; no currency column | untested |
| A13 | **Coverage limitation: model-canonical crawling only reaches models that have schematic `/Sections/` pages.** Confirmed across brands during the widened crawl prep (2026-06-12): many *newer* models (e.g. Maytag MDB7749SBW, KitchenAid KUDS30IXSS, GE GDF520PGJWW, Frigidaire FFCD2413US) expose only a **flat parts list** (`/Models/{n}/Parts/`, the robots-discouraged mega-list) with **zero `/Sections/` pages**, so the section-based compatibility ingestion (A9) yields no pairs for them. Section-having models skew older, which also skews brand variety (Whirlpool/Frigidaire/GE had crawlable models in our checks). **Product boundary:** the agent can only answer compatibility for models in the catalog, and the catalog can only cover models with section pages via the current path. **Extension path:** add a second ingestion path that parses the flat parts list directly (from `/Models/{n}/Parts/` — would require revisiting the A9 deny-glob and the per-page janitor, since flat-list pairs would be sourced from the parts page, not a section). Until then, seed sets must use section-having models, and "model not found / not covered" is the honest answer for flat-list-only models. | active |
| A12 | **`Cover-Sheet` sections legitimately have no parts.** The first live crawl flagged every model's `/Sections/Cover-Sheet/` page as drift (`parts missing`) — these are the schematic *diagram cover* pages, not parts lists, so they correctly have zero parts. **RESOLVED 2026-06-12:** `section.py` has `KNOWN_EMPTY_SECTIONS = {cover-sheet}`; those slugs skip the `require(parts)` assertion and return empty parts (no alert), while genuine parts sections with a broken selector still raise `SchemaDriftError` (both behaviours tested). Add to the set if other no-parts section types are confirmed. | resolved |
| A11 | **Aurora 0-ACU cold start drops in-flight transactions.** With `min_capacity=0` (auto-pause), the first connection after idle triggers a ~15–60s resume; connections/queries during the resume window fail with `FATAL: the database system is starting up` or `SSL error: unexpected eof`. Confirmed 2026-06-12 during the indexing proof. **Every service that connects to Aurora must implement connection retry with backoff** (a warm-up SELECT or psycopg retry) — this is a Phase 2 requirement for gateway/catalog/orchestrator/etl, not optional. Raising the floor to 0.5 ACU avoids it at ~+$43/mo (we keep 0 for cost; retry is the right fix). | active |
| A10 | **Section URLs must keep their query string.** `/Models/{n}/Sections/{s}/` returns **HTTP 500** without the `?ModelID=…&ModelNum=…&Type=…` params present in the model page's section links. The model parser preserves the full href (query included); the fetcher must not strip it. A future "URL cleanup" that drops query strings would silently break section fetching (the compat completeness path). Verified 2026-06-12: bare section URL → 500, full URL → 200. | active |
| A9 | **Compatibility directionality**: does PartSelect list compatible models on part pages, compatible parts on model pages, or both — and which side is complete? | The table is direction-agnostic (a pair observed on whatever page), but the per-page staleness janitor assumes **one canonical ingestion direction**: a pair carries one `source_page_id`, so ingesting the same pair from both directions makes page A's janitor able to delete a pair still live on page B. | **RESOLVED 2026-06-12 — model-canonical.** Both directions exist (verified on real HTML): part pages list models, model pages list parts, and a sampled pair cross-attests. BUT the part→models list is **paginated/incomplete per fetch** (PS11752778 page 1 = first 30 Kenmore models, omits a known-compatible Whirlpool model on a later page), while the model→parts list is **bounded and complete per model page**. Decision: **compatibility pairs are ingested ONLY from model pages** (one `source_page_id` per pair, janitor sound); part pages supply attributes only and their cross-reference is ignored for pair ingestion. Enforced as a parser rule — **no schema change to 0001**. |

## Phases & exit criteria (work strictly in order)

| Phase | Scope | Exit criteria — stop and review here |
|---|---|---|
| 0 | Monorepo scaffold; Terraform bootstrap (state backend, VPC, EKS, ECR, IRSA); platform namespace (ingress, cert-manager, external-secrets); CI skeleton; local compose profile | `terraform apply` clean; hello-world pod behind TLS ingress; `make check` green |
| 1 | Aurora schema (parts, models, part_model_compatibility, repair_guides, qna, orders); crawler+parsers on ~500 seed pages; S3/SQS plumbing; OpenSearch indexing | Compatibility answerable via SQL; hybrid search returns relevant guides; pipeline dashboard panels live |
| 2 | LangGraph graph, tools, guardrails, Redis memory, FastAPI SSE gateway | 3 brief examples + 20 eval cases pass via curl; traces visible in Jaeger |
| 3 | Next.js chat UI: streaming, product cards, quick replies, feedback | E2E demo on real data |
| 4 | Full observability: 5 Grafana dashboards, Kibana logs, Alertmanager→Slack, Langfuse (if O3=yes) | One trace end-to-end; test alert fires in Slack |
| 5 | 100-case eval suite as CI gate; semantic cache; crawl widening; canary deploys; admin views | CI eval gate green; cost dashboard live |

## Reference
- Inspiration repo (do NOT copy architecture): https://github.com/zehuiwu/partselect-agent
- Case-study brief: docs/PRD.md §1, §10
